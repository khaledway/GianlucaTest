@model  List<WebApplication3.DAL.Employee>
@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>



<p>
    @Html.ActionLink("Create New Employee", "Create")
</p>

<table class="table">
    <tr>
        <th>
            Emloyee ID
        </th>

        <th>
            Emloyee Name
        </th>

        <th>
            HireDate
        </th>
        <th>
            Department Name
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmloyeeID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmloyeeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HireDate)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.Department.DepartmentName)
            </td>

            <td>
                @*@Html.ActionLink("Edit", "Edit", new { id = item.EmloyeeID }) |
            @Html.ActionLink("Details", "Details", new { id = item.EmloyeeID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.EmloyeeID })*@
            </td>
        </tr>
    }

</table>



